// @flow

// ------------------------------------
// Reducer
// ------------------------------------
const initialState: Object = {
  name: ''
};

export default function(state: Object = initialState, action: Object) {

  // let newState;
  //
  // switch (action.type) {
  // }

  return state;
}
